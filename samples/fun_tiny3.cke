let sum = (a: int, b: int): int => {
  if (a > 0) { sum(a - 1, b + 1) } else { b }
}
print(sum(2, 3))

let sum2 = fun (ax) { fun (bx) { ax + bx } }
print(sum2(1)(2))

let fib = (a: int) => {
  if (a > 1) { fib(a - 1) + fib(a - 2) } else { a }
}
fib(0)
